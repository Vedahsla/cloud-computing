const authRouter = require('./auth');
const reviewsRouter = require('./reviews');
const routesRouter = require('./routes');

module.exports = {
  authRouter,
  reviewsRouter,
  routesRouter,
};